import 'package:flutter/material.dart';

// Core colors
const errorColor = Colors.redAccent;
const primaryColor = Colors.black;
const surfaceColor = Colors.white;
final secondaryColor = Colors.grey[600];
const successColor = Colors.greenAccent;

// Additional theme colors
const primaryPurple = Color(0xFF000000);
const textSecondary = Color(0xFF757575);
const cardBackground = Color(0xFFF5F5F5);
/// Star (like) color — used for food-style rating in the feed.
const starGold = Color(0xFFFFB300);
const downvoteRed = Color(0xFFF44336);
const accentPink = Colors.black;
const selectedTab = Color.fromARGB(255, 44, 44, 44);
const unselectedTab = Color(0xFF9E9E9E);
const textPrimary = Colors.black;

// Gradient
const backgroundGradient = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  colors: [Color(0xFFF5F5F5), Color(0xFFFFFFFF)],
);