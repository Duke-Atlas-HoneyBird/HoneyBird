import 'package:flutter/material.dart';

// Spacing constants
const double spacingXs = 4.0;
const double spacingS = 8.0;
const double spacingM = 16.0;
const double spacingL = 24.0;
const double spacingXl = 32.0;

// Legacy padding/margin (can be deprecated later)
var primaryPadding = const EdgeInsets.all(16);
var primaryMargin = const EdgeInsets.all(16);